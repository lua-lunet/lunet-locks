# lunet-locks

Lightweight advisory locking service using expiring leases.

Pre-alpha sketch: currently a Rust VRR core behind LuaJIT FFI for lunet and Teal. There is no
runnable locking service yet. See <https://simbo1905.github.io/lunet-locks/>.
